{
  "name": "serf",
  "version": "2.0.0",
  "description": "Serf — dark factory for coding agents. Master serf + GAN critic + board. The folder is the state.",
  "type": "module",
  "bin": {
    "serf": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --banner '#!/usr/bin/env bun' && chmod +x dist/index.js",
    "test": "bun test tests/",
    "start": "bun run src/index.ts",
    "health": "bun run scripts/health-check.ts",
    "health:strict": "bun run scripts/health-check.ts --strict",
    "health:gan": "bun run scripts/health-check.ts --gan"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun": "^1.3.14"
  },
  "license": "MIT",
  "engines": {
    "bun": ">=1.0.0"
  }
}